/**
 * ⚠️  GENERATED — NEVER EDIT BY HAND.
 *
 *     npx supabase gen types typescript --linked > src/server/database/types.ts
 *
 * Run after EVERY migration.
 *
 * ONE repo now means ONE types file. Before the merge this had to be generated
 * twice and the two copies drifted. That drift is precisely how the Muddarris
 * currency bug survived code review.
 */
export type Database = any;
